#include <stdio.h>

/**
* main - prints all single digits fo base 10 
* from 0 through 10, followed by a new line
*Return: Always 0 (Success)
*/
int main(void)
{
	int num;

	for (num = 0; num < 10; num++) 
	{
		printf("%d", num);

	}
	return (0);
}

